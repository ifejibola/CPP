

class Counter
{
public:
    // 3 member functions
    Counter();              // initialization
    int getCount();         //Get the current count
    void increateBy(int x); // Add x to the count

private:
    int count;
};