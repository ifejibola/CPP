// Declaration : is a statement that introduces a name into the program.
//  It specifies a type for the named entity :
// # A type define s aset of possible values and a set of operations(for an object)
// # An object is some memory that holds a value of some type.
// # a value is a set of bits interpreted according to a type
// # a variable is a named object.

// FOR  a function to be usable ina  constant expression, that is, in an expression that will be
// evaluated by the compiler, it must be defined constexpr
#include <iostream>
// constexpr double square(double x) { return x * x; }